"""Extract rtw8822b init tables from kernel C source to Python data files.

Source: driver_sources/rtw88-source-v6.18/rtw8822b_table.c
Output: src/airscope/chips/rtl8822bu/assets/{mac,agc,bb,rf_a,rf_b}_tbl.py

The five tables are arrays of u32 in C, walked 2 at a time by the rtw88
phy_cond runtime (see :mod:`airscope.chips.rtw88_base.phy_cond`).

Usage:
    .venv/Scripts/python.exe scripts/chips/rtl8822bu/extract_init_tables.py
"""
from __future__ import annotations

import re
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parents[3]
SRC = ROOT / "driver_sources/rtw88-source-v6.18/rtw8822b_table.c"
OUT_DIR = ROOT / "src/airscope/chips/rtl8822bu/assets"

TABLES = ("mac", "agc", "bb", "rf_a", "rf_b")


def parse_table(text: str, name: str) -> list[int]:
    """Return the flat u32 list for `static const u32 rtw8822b_<name>[] = { ... };`."""
    pat = re.compile(
        rf"static\s+const\s+u32\s+rtw8822b_{re.escape(name)}\[\]\s*=\s*\{{(.*?)\}}\s*;",
        re.DOTALL,
    )
    m = pat.search(text)
    if not m:
        raise SystemExit(f"rtw8822b_{name} not found in source")
    body = m.group(1)
    body = re.sub(r"/\*.*?\*/", "", body, flags=re.DOTALL)
    body = re.sub(r"//.*", "", body)
    return [int(tok, 16) for tok in re.findall(r"0[xX][0-9A-Fa-f]+", body)]


def count_markers(words: list[int]) -> dict[str, int]:
    counts = {"cfg": 0, "if": 0, "elif": 0, "else": 0, "endif": 0, "neg": 0}
    i, n = 0, len(words)
    while i + 1 < n:
        w0 = words[i]
        pos = (w0 >> 31) & 1
        neg = (w0 >> 30) & 1
        if pos:
            branch = (w0 >> 28) & 3
            counts[("if", "elif", "else", "endif")[branch]] += 1
        elif neg:
            counts["neg"] += 1
        else:
            counts["cfg"] += 1
        i += 2
    return counts


HEADER_TMPL = '''"""rtw8822b {name} init table — extracted from kernel rtw8822b_table.c.

Auto-generated by scripts/chips/rtl8822bu/extract_init_tables.py — DO NOT EDIT.

Flat u32 list, walked 2 at a time by `airscope.chips.rtw88_base.phy_cond`.
{summary}
"""

TABLE: list[int] = [
'''


def emit(name: str, words: list[int]) -> None:
    counts = count_markers(words)
    summary = (
        f"cfg={counts['cfg']}  IF={counts['if']}  ELIF={counts['elif']}  "
        f"ELSE={counts['else']}  ENDIF={counts['endif']}  neg={counts['neg']}  "
        f"u32s={len(words)}"
    )
    OUT_DIR.mkdir(parents=True, exist_ok=True)
    out = OUT_DIR / f"{name}_tbl.py"
    lines = [HEADER_TMPL.format(name=name, summary=summary)]
    for k in range(0, len(words), 2):
        a, b = words[k], words[k + 1]
        lines.append(f"    0x{a:08X}, 0x{b:08X},\n")
    lines.append("]\n")
    out.write_text("".join(lines), encoding="utf-8")
    print(f"  {name:5s} -> {out.relative_to(ROOT)}  ({summary})")

    if counts["if"] + counts["elif"] != counts["neg"]:
        print(f"    WARN: IF+ELIF ({counts['if']+counts['elif']}) != neg ({counts['neg']})")
    if counts["if"] != counts["endif"]:
        print(f"    WARN: IF ({counts['if']}) != ENDIF ({counts['endif']})")


def main() -> int:
    if not SRC.exists():
        print(f"ERROR: missing {SRC}", file=sys.stderr)
        return 1
    text = SRC.read_text(encoding="utf-8", errors="replace")
    print(f"Reading {SRC.relative_to(ROOT)}")
    for name in TABLES:
        words = parse_table(text, name)
        emit(name, words)
    return 0


if __name__ == "__main__":
    sys.exit(main())
